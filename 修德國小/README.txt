外部GEXF版網站包 v5（手機節點避擠 + 觸控縮放）

本次調整：
- 手機版預設縮得更開，避免節點一開始過度密集
- 新增手機觸控操作：
  - 單指拖曳空白處：平移
  - 單指拖曳節點：移動節點
  - 雙指開合：縮放
- PC版維持原本行為
- 預設展示仍為左右並列

使用方式：
1. index.html與network.gexf需放在同一資料夾
2. 建議用HTTP伺服器開啟，例如：
   python -m http.server 8000
3. 瀏覽器開啟：
   http://localhost:8000
